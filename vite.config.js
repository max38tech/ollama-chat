import { sveltekit } from '@sveltejs/kit/vite';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [sveltekit()],

  // This new section explicitly tells Vite to exclude the Tauri API from its dependency optimization process.
  optimizeDeps: {
    exclude: ["@tauri-apps/api"],
  },

  clearScreen: false,
  server: {
    port: 1420,
    strictPort: true,
    watch: {
      ignored: ["**/src-tauri/**"],
    },
  },
});